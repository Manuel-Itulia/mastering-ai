def assess_day(steps):
    return steps >= 10000
def assess_day(steps, goal=10000):
    return steps >= goal

#On that branch, create a file called report.py and add two lines of code. 
# Commit it. Switch back to main and confirm report.py is gone. 
#Switch back to the feature branch and confirm it returns. Then merge the branch into main and delete the feature branch.